import { HotspotDrawingDirective } from './hotspot-drawing.directive';

describe('HotspotDrawingDirective', () => {
  it('should create an instance', () => {
    const directive = new HotspotDrawingDirective();
    expect(directive).toBeTruthy();
  });
});
